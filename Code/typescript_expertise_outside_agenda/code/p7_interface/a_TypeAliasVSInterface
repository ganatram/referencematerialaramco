// eg 1 similarity between type alias and interface

type Poet = {
  born: number;
  name: string;
};

interface Poet1 {
  born: number;
  name: string;
}

let valueLater: Poet;

// Ok
valueLater = {
  born: 1935,
  name: "Sara Teasdale",
};

valueLater = "Emily Dickinson";
// Error: Type 'string' is not assignable to 'Poet'.

valueLater = {
  born: true,
  // Error: Type 'boolean' is not assignable to type 'number'.
  name: "Sappho",
};

// will understand differences between these later today
